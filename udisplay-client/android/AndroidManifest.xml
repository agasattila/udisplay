<?xml version="1.0" encoding="utf-8"?>
<!--
  uDisplay Android manifest.

  androiddeployqt does NOT merge this file with a Qt-generated base manifest —
  when QT_ANDROID_PACKAGE_SOURCE_DIR/AndroidManifest.xml exists, androiddeployqt
  copies Qt's template dir into the build output first, then overwrites it
  file-for-file with this directory's contents. This file IS the complete
  manifest used to build the APK, so it must declare everything: permissions,
  the <application> block, and the launcher <activity>.

  Android 6–11: BLUETOOTH + BLUETOOTH_ADMIN + ACCESS_FINE_LOCATION (required for
    scanning even when not using location — Android platform requirement).
  Android 12+ (API 31+): BLUETOOTH_SCAN + BLUETOOTH_CONNECT replace BLUETOOTH_ADMIN.
    Location is NOT a general Android 12+ BLE requirement: an app that declares
    BLUETOOTH_SCAN with usesPermissionFlags="neverForLocation" scans without it.
    uDisplay does not set that flag (we use the BT address as the stable uniqueId
    for deduplication), and without it Android 12+ still delivers scan results
    only to apps holding ACCESS_FINE_LOCATION. So this is a uDisplay-specific
    requirement on API 31+. It must be PRECISE: an approximate-only (COARSE) grant
    still yields zero results. FINE and COARSE are both declared because Android
    recommends requesting them together on 31+ (Qt's QLocationPermission::Precise
    does so).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Legacy Bluetooth (Android 6–11) -->
    <uses-permission android:name="android.permission.BLUETOOTH"
                     android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
                     android:maxSdkVersion="30" />

    <!-- Android 12+ (API 31+) runtime permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
                     android:minSdkVersion="31" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
                     android:minSdkVersion="31" />

    <!-- Location: Android requirement for BLE scan on API <= 30; uDisplay-specific
         requirement on API 31+ (no neverForLocation flag, see header comment) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Declare BLE feature requirement so the Play Store filters correctly -->
    <uses-feature android:name="android.hardware.bluetooth_le"
                  android:required="true" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="org.qtproject.qt.android.bindings.QtApplication"
        android:label="uDisplay"
        android:icon="-- %%INSERT_APP_ICON%% --"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:allowBackup="true"
        android:usesCleartextTraffic="true">

        <activity
            android:name="org.qtproject.qt.android.bindings.QtActivity"
            android:label="uDisplay"
            android:screenOrientation="unspecified"
            android:configChanges="orientation|uiMode|screenLayout|screenSize|smallestScreenSize|layoutDirection|locale|fontScale|keyboard|keyboardHidden|navigation|mcc|mnc|density"
            android:launchMode="singleTop"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="udisplay-client" />

        </activity>

    </application>

</manifest>
